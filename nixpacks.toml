# Railway deployment configuration
[phases.setup]
aptPkgs = ["ffmpeg"]
nixPkgs = ["python313"]

[phases.install]
dependsOn = ["setup"]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python app.py"
